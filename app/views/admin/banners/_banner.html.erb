<% index = banner_counter + 0 %>
<tr>
  <td><%= index+1 %></td>
  <td><%= banner.title %></td>
  <td><%= banner.user.callsign.upcase %></td>
  <td>
    <%= link_to edit_admin_banner_path(banner), class: "btn btn-primary btn-sm" do %>
      <i class="fa fa-pencil" aria-hidden="true"></i>
    <% end %>
    <%= link_to admin_banner_path(banner), class: "btn btn-secondary btn-sm" do %>
      <i class="fa fa-share" aria-hidden="true"></i> view
    <% end %>
  </td>
</tr>
