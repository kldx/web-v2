<h3>Comments</h3>
<%= form_for [commentable, Comment.new], remote: true do |f| %>
  <div class="form-group">
    <%= f.text_area :body, class: "form-control", placeholder: "Add a comment", rows: 4 %>
  </div>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
